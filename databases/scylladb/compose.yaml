services:
  scylladb:
    image: scylladb/scylla:5.2.0
    ports:
      - "9042:9042"
    command: --smp 1 --memory 750M --overprovisioned 1
    healthcheck:
      test: ["CMD-SHELL", "/opt/scylladb/bin/nodetool status"]
      interval: 30s
      timeout: 10s
      retries: 5
